---
- name: Execute Initial Setup Commands
  hosts: localhost
  become: true
  tasks:
    - name: Ensure necessary directories exist
      file:
        path: "{{ item }}"
        state: directory
      loop:
        - "/etc/systemd/system/getty@tty1.service.d/"

    - name: Add cloud-init wait configuration
      blockinfile:
        path: "/etc/systemd/system/getty@tty1.service.d/cloud-init_wait.conf"
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK - cloud-init wait"
        block: |
          [Unit]
          After=cloud-final.service

    - name: Install necessary packages
      apt:
        name:
          - needrestart
          - cowsay
          - fortune-mod
          - python3
          - python3-pip
          - python3-docker
          - python3-venv
          - iftop
          - net-tools
          - unzip
          - build-essential
          - micro
          - duf
          - tldr
          - fzf
          - bat
          - fd-find
          - libpam-modules        # added
          - libpam-modules-bin    # added
        state: present
        update_cache: true

    - name: Get latest fastfetch release info
      uri:
        url: https://api.github.com/repos/fastfetch-cli/fastfetch/releases/latest
        return_content: true
      register: fastfetch_release

    - name: Download fastfetch deb package
      get_url:
        url: "{{ (fastfetch_release.json.assets | selectattr('name', 'search', 'linux-amd64\\.deb') | list | first).browser_download_url }}"
        dest: /tmp/fastfetch.deb
        mode: '0644'

    - name: Install fastfetch from deb
      apt:
        deb: /tmp/fastfetch.deb
        state: present

    - name: Ensure needrestart.conf restart setting is set to 'a'
      lineinfile:
        path: "/etc/needrestart/needrestart.conf"
        regexp: '^\s*\$nrconf{restart}\s*='
        line: "$nrconf{restart} = 'a';"
        state: present
        backrefs: yes

    - name: Customize /etc/screenrc
      blockinfile:
        path: "/etc/screenrc"
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK - screenrc"
        block: |
          startup_message off
          nethack on
          vbell_msg ''
          termcapinfo *  vb=:
          altscreen on
          
          # Enable arrow keys for shell history navigation
          bindkey -k up send-up
          bindkey -k dn send-down

    - name: Copy modified /etc/screenrc to /root/.screenrc
      copy:
        src: "/etc/screenrc"
        dest: "/root/.screenrc"
        remote_src: yes

    - name: Get list of home directories
      find:
        paths: /home
        file_type: directory
        recurse: no
      register: user_dirs

    - name: Copy .screenrc to all home directories
      copy:
        src: "/etc/screenrc"
        dest: "/home/{{ item.path | basename }}/.screenrc"
        owner: "{{ item.path | basename }}"
        group: "{{ item.path | basename }}"
      loop: "{{ user_dirs.files }}"
      loop_control:
        label: "{{ item.path }}"

    - name: Create .inputrc for root user
      copy:
        dest: "/root/.inputrc"
        owner: root
        group: root
        mode: '0644'
        content: |
          # Completion settings
          set completion-ignore-case on
          set show-all-if-ambiguous on
          set show-all-if-unmodified on

          # Completion display improvements
          set completion-display-width 0
          set colored-completion-prefix on
          set colored-stats on
          set menu-complete-display-prefix on

          # Editing improvements
          set enable-bracketed-paste-mode on
          set enable-keypad on
          set visible-stats on

          # History settings
          set history-preserve-point on
          set history-size 10000

          # Filtered history search (Alt+Up/Down for commands starting with typed text)
          "\e[1;3A": history-search-backward
          "\e[1;3B": history-search-forward

          # Standard bindings
          # Ctrl+Left/Right for word navigation
          "\e[1;5C": forward-word
          "\e[1;5D": backward-word

          # Home/End for line navigation
          "\e[H": beginning-of-line
          "\e[F": end-of-line

          # Delete key
          "\e[3~": delete-char

          # Helpful completions
          set match-hidden-files on

    - name: Create .inputrc for all home directories
      copy:
        dest: "/home/{{ item.path | basename }}/.inputrc"
        owner: "{{ item.path | basename }}"
        group: "{{ item.path | basename }}"
        mode: '0644'
        content: |
          # Completion settings
          set completion-ignore-case on
          set show-all-if-ambiguous on
          set show-all-if-unmodified on

          # Completion display improvements
          set completion-display-width 0
          set colored-completion-prefix on
          set colored-stats on
          set menu-complete-display-prefix on

          # Editing improvements
          set enable-bracketed-paste-mode on
          set enable-keypad on
          set visible-stats on

          # History settings
          set history-preserve-point on
          set history-size 10000

          # Filtered history search (Alt+Up/Down for commands starting with typed text)
          "\e[1;3A": history-search-backward
          "\e[1;3B": history-search-forward

          # Standard bindings
          # Ctrl+Left/Right for word navigation
          "\e[1;5C": forward-word
          "\e[1;5D": backward-word

          # Home/End for line navigation
          "\e[H": beginning-of-line
          "\e[F": end-of-line

          # Delete key
          "\e[3~": delete-char

          # Helpful completions
          set match-hidden-files on
      loop: "{{ user_dirs.files }}"
      loop_control:
        label: "{{ item.path }}"

    - name: Create .local/share directory for each user
      file:
        path: "/home/{{ item.path | basename }}/.local/share"
        state: directory
        owner: "{{ item.path | basename }}"
        group: "{{ item.path | basename }}"
        mode: '0755'
      loop: "{{ user_dirs.files }}"
      loop_control:
        label: "{{ item.path }}"

    - name: Create .local/share directory for root user
      file:
        path: "/root/.local/share"
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Append custom commands to each user's .bashrc
      blockinfile:
        path: "/home/{{ item.path | basename }}/.bashrc"
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK - MOTD + fzf helper"
        block: |
          # Added by Ansible for MOTD
          export PATH="/usr/games:$PATH"
          clear
          fastfetch
          alias dfl='duf --only local'
          alias bat="batcat"
          alias fd="fdfind"
          alias apb="ansible-playbook"
          export FZF_DEFAULT_COMMAND='fd --type f'  
          export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"

          # fzf + nano helper
          fz () {
            local dir="${1:-.}"
            local file
            file=$(find "$dir" -type f 2>/dev/null | fzf --preview 'bat --style=plain --color=always {}') || return
            sudo nano "$file"
          }
        owner: "{{ item.path | basename }}"
        group: "{{ item.path | basename }}"
      loop: "{{ user_dirs.files }}"
      loop_control:
        label: "{{ item.path }}"

    - name: Append custom commands to root's .bashrc
      blockinfile:
        path: "/root/.bashrc"
        create: yes
        marker: "# {mark} ANSIBLE MANAGED BLOCK - MOTD + fzf helper"
        block: |
          # Added by Ansible for MOTD
          export PATH="/usr/games:$PATH"
          clear
          fastfetch
          alias dfl='duf --only local'
          alias bat="batcat"
          alias fd="fdfind"
          alias apb="ansible-playbook"
          export FZF_DEFAULT_COMMAND='fd --type f'
          export FZF_CTRL_T_COMMAND="$FZF_DEFAULT_COMMAND"

          # fzf + nano helper
          fz () {
            local dir="${1:-.}"
            local file
            file=$(find "$dir" -type f 2>/dev/null | fzf --preview 'bat --style=plain --color=always {}') || return
            sudo nano "$file"
          }
        owner: root
        group: root